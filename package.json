{
  "name": "video-rag-desktop",
  "version": "1.0.0",
  "description": "Electron + React + FastAPI Video Semantic Search Application",
  "main": "electron/main.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "electron": "electron .",
    "start": "npm run dev & npm run electron",
    "install-backend": "cd backend && pip install -r requirements.txt"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "electron": "^29.1.0",
    "electron-builder": "^24.13.3"
  },
  "dependencies": {
    "electron-log": "^5.1.1"
  }
}
